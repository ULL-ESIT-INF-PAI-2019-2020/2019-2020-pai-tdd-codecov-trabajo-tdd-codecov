{
  "name": "test",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "directories": {
    "lib": "lib",
    "test": "test"
  },
  "scripts": {
    "test": "nyc mocha test/code-coverage",
    "coverage": "nyc --reporter html npm test",
    "report-coverage": "nyc report --reporter lcovonly > coverage.lcov && codecov"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "babel-plugin-istanbul": "^6.0.0",
    "chai": "^4.2.0",
    "nyc": "^15.0.0"
  },
  "dependencies": {
    "mocha": "^7.1.1"
  }
}
